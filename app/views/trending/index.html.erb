<div class="col-md-6 col-md-offset-3">

<h1>What's Trending?</h1>
<%= @title %>

<% if @results != nil %>
  <ul>
  <% @results["works"].first(10).each do |book| %>
  <li>
    <div>
        <h2><%= book["title"] %></h2>
        <% end %>
    </div>
  </li>
  <% end %>
  </ul>
</div>